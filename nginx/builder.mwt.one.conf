# DEPRECATED — este archivo NO lo copia el Dockerfile.
# Se mantiene como referencia histórica. La config activa es ./default.conf.
# El SSL termina en mwt-nginx (/opt/mwt) que hace proxy a 127.0.0.1:8080.
