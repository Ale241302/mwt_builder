server {
    listen 80;
    server_name builder.mwt.one;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'Nginx is waiting for SSL certificates...';
        add_header Content-Type text/plain;
    }
}
